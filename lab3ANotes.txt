BC436A10
-Use DDS to create a display file using RDI. We will design the screen with RDI tool.

call realestat2 -> call this to view an example of what I need to produce for lab3A

*** for the test make sure to know pp slide#9 about RPG ***
-RPG is a unique programming language used by IBM

IMPORTANT COMMANDS:
-> DCL-F INCOMETDSP workstn; // means this is a display file // declare file

EXFMT -> 

WRITE -> 

WRITE RECORD1;

EXFMT RECORD2;

->  DCL-S PREMIUM PACKED(4:2); // to declare a variable name

Boolean variables -predefined
-use *IN00 - *IN24 to map 24 function keys in display file.
-*INLR - Last Record Indicator // how we end a RPG program
-boolean variables can have the values '0' or *OFF 
				       '1' or *ON
IF Statements:
-can use operators: =, <, <=, <>, NOT, AND, OR

example
IF Test1 < Test2
	Minimum = Test1;
ENDIF;
-------------------------

IF Test1 < Test2
	Minimum = Test1;
ELSE
	Minimum = Test2;
ENDIF;
-------------------------

Loops:
- Do while // condition will be checked at the beginning of the loop.
e.g. 

DOW (a <=5) AND (b + c = 0); 
	EXSR nextRecord;
	ENDDO;
- DO UNTIL // condition will be checked at the end of the loop.

e.g.
	DOU X > 10;
	Total = Total + Array(x);
	X - X + 1;
	ENDDO;

Execute a subroutine:
e.g. EXSR GETGRADE;
     	TOTALMARK = 90;
    	LETGRADE = 'A+';
    ENDSR;


** TO INDICATE THE END OF A RPG PROGRAM:
-> *INLR = *ON; // wright these lines at the end of an RPG program.
    RETURN;
-------------------------------------------------------------------

lab 3A:
- the record has 2 fields
1) SALEINFO
2) RESULT